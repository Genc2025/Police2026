import React,{useState}from'react';import{countries}from'../data/countries.js';export default function CountryCarousel(){const[selected,setSelected]=useState('US');return <><div className="carousel">{countries.map((c,i)=><button key={c.code} className={'country-card '+(selected===c.code?'active':'')} style={{'--offset':i-2}} onClick={()=>setSelected(c.code)}><span>{c.flag}</span><b>{c.name}</b>{selected===c.code&&<i>✓</i>}</button>)}</div><div className="dots">{countries.map(c=><em key={c.code} className={selected===c.code?'on':''}/>)}</div></>}